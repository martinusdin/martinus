version: 2 
 
jobs:
  build:
    parallelism: 1
    docker:
      - image: cimg/ruby:3.0.2-node
    steps: 
      - checkout 
      - run: 
          name: Run tests 
          command: chmod +x raceds.sh && ./raceds.sh
